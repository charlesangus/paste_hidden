---
phase: 08-test-infrastructure-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/stubs.py
  - tests/conftest.py
  - tests/__init__.py
  - tests/test_anchor_color_system.py
  - tests/test_anchor_navigation.py
  - tests/test_cross_script_paste.py
  - tests/test_dot_anchor_name_sync.py
  - tests/test_dot_type_distinction.py
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "Flat discovery (pytest tests/ or python3 -m unittest discover) completes with zero errors and zero failures"
    - "tests/conftest.py exists and installs all stubs at module level before any test file is imported"
    - "tests/stubs.py exists and exports StubKnob, StubNode, make_stub_nuke_module(), make_stub_nukescripts_module()"
    - "All five affected test files contain no per-file stub installation blocks"
    - "All individual test files continue to pass when run in isolation"
  artifacts:
    - path: "tests/stubs.py"
      provides: "Superset StubKnob, StubNode, nuke/nukescripts stub factories"
      exports: ["StubKnob", "StubNode", "make_stub_nuke_module", "make_stub_nukescripts_module"]
    - path: "tests/conftest.py"
      provides: "Module-level sys.modules stub installation for pytest flat discovery"
    - path: "tests/__init__.py"
      provides: "Module-level sys.modules stub installation for unittest discover path"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/stubs.py"
      via: "from stubs import make_stub_nuke_module, make_stub_nukescripts_module"
      pattern: "from stubs import"
    - from: "tests/conftest.py"
      to: "sys.modules['nuke']"
      via: "module-level assignment before test file collection"
      pattern: "sys\\.modules\\['nuke'\\] = make_stub_nuke_module"
    - from: "tests/__init__.py"
      to: "sys.modules['nuke']"
      via: "module-level assignment, imported before test files in unittest discover"
      pattern: "sys\\.modules\\['nuke'\\] = make_stub_nuke_module"
---

<objective>
Centralize Qt, tabtabtab, nuke, and nukescripts stub installation into shared modules
(tests/stubs.py and tests/conftest.py) so that flat-discovery test runs complete with
zero errors, then strip all per-file stub blocks from the five affected test files.

Purpose: The test suite currently has 4-8 spurious errors under flat discovery because
whichever test file is imported first wins the sys.modules race. Once the stub set is
richest (superset across all 5 files) and installed before any test file is imported,
the ordering conflict cannot occur.

Output: tests/stubs.py (new), tests/conftest.py (new), tests/__init__.py (updated),
five test files with per-file stub blocks removed. Zero errors under flat discovery.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-test-infrastructure-stabilization/08-CONTEXT.md
@.planning/phases/08-test-infrastructure-stabilization/08-RESEARCH.md

<interfaces>
<!-- Key stub API surface synthesized from all 5 test files. Executor uses these directly. -->

StubKnob superset (from RESEARCH.md synthesis):
  - __init__(self, value='') — _value=value, _visible=True
  - getText(self) → str(self._value)   [str() coercion: safer superset]
  - setValue(self, value)
  - getValue(self) → self._value
  - value(self) → self._value
  - setVisible(self, visible)          [dot files need this]
  - setFlag(self, flag)                [no-op; dot files need this]
  - setText(self, value)               [alias setValue; dot files need this]

StubNode superset (from RESEARCH.md synthesis):
  - __init__(self, name='Node', node_class='NoOp', xpos=0, ypos=0, knobs_dict=None)
    — _set_name_calls=[] (for dot_anchor_name_sync tracking)
  - name(), fullName(), Class(), xpos(), ypos(), knobs()
  - screenWidth() → 100, screenHeight() → 50  [color_system, navigation]
  - input(index), setInput(index, node), setXYpos(x, y)
  - setName(name) — updates _name AND appends to _set_name_calls
  - addKnob(knob) — stores knob: self._knobs[knob.name()] = knob  [functional wins]
  - removeKnob(knob) — no-op pass
  - __getitem__(knob_name) — raises KeyError if missing
  - __setitem__(knob_name, value)

make_stub_nuke_module() returns types.ModuleType('nuke') with:
  StubNode, StubKnob class refs, root (returns MagicMock with name()='destScript.nk'),
  allNodes=MagicMock(return_value=[]), toNode=MagicMock(return_value=None),
  createNode=MagicMock(), selectedNodes=MagicMock(return_value=[]),
  nodeCopy=MagicMock(), nodePaste=MagicMock(return_value=None),
  exists=MagicMock(return_value=False), delete=MagicMock(),
  INVISIBLE=0, NUKE_VERSION_MAJOR=16,
  PyScript_Knob=MagicMock(), String_Knob=MagicMock(side_effect=lambda name,*a: StubKnob()),
  Tab_Knob=MagicMock(side_effect=lambda name,*a: StubKnob()),
  Boolean_Knob=MagicMock(side_effect=lambda name,*a: StubKnob()),
  zoom=MagicMock(return_value=1.0), center=MagicMock(return_value=[0.0,0.0]),
  zoomToFitSelected=MagicMock(), getColor=MagicMock(return_value=0)

make_stub_nukescripts_module() returns types.ModuleType('nukescripts') with:
  cut_paste_file=lambda: '/tmp/nuke_stub_clipboard.nk',
  clear_selection_recursive=MagicMock()

conftest.py PySide6 pattern — MUST use MagicMock (NOT types.ModuleType) for sub-modules:
  sys.modules['PySide6'] = MagicMock(name='PySide6')
  sys.modules['PySide6.QtCore'] = MagicMock(name='PySide6.QtCore')
  sys.modules['PySide6.QtGui'] = MagicMock(name='PySide6.QtGui')
  sys.modules['PySide6.QtWidgets'] = MagicMock(name='PySide6.QtWidgets')
  sys.modules['PySide6'].QtCore.Qt = MagicMock()

Stub blocks to remove from each test file span from:
  - test_anchor_color_system.py:   lines 22-180 (the Qt/tabtabtab comment block through
    sys.modules['nukescripts'].clear_selection_recursive = MagicMock())
    IMPORTANT: lines 182+ (colors stub sequence starting with the next comment block)
    must NOT be removed — that is test-file-specific logic unrelated to the ordering fix.
  - test_anchor_navigation.py:     lines 22-191 (Qt/tabtabtab comment through
    sys.modules['nukescripts'] = _nukescripts_stub)
    IMPORTANT: _ensure_qt_stubs_support_mock_attributes() helper function and all
    subsequent class definitions must remain. Only remove the top stub installation block.
  - test_cross_script_paste.py:    lines 20-140 (Qt/tabtabtab comment through
    sys.modules['nukescripts'].clear_selection_recursive = MagicMock())
  - test_dot_anchor_name_sync.py:  lines 22-170 (Qt/tabtabtab comment through
    sys.modules['nukescripts'].clear_selection_recursive = MagicMock())
  - test_dot_type_distinction.py:  lines 24-173 (Qt/tabtabtab comment through
    sys.modules['nukescripts'].clear_selection_recursive = MagicMock())

test_prefs.py: DO NOT modify. Its guard `if 'nuke' not in sys.modules:` will
  evaluate False once conftest/init installs nuke first. No changes needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create tests/stubs.py with superset stub classes and factory functions</name>
  <files>tests/stubs.py</files>
  <action>
Create /workspace/tests/stubs.py as a new file containing the superset stub library.

The file must define:

1. StubKnob class (superset of all 5 per-file definitions):
   - __init__(self, value='') with _value=value and _visible=True
   - getText(self) returning str(self._value) — use str() coercion (safer superset)
   - setValue(self, value) and getValue(self)
   - value(self) returning self._value
   - setVisible(self, visible) storing self._visible = visible
   - setFlag(self, flag) as no-op pass
   - setText(self, value) as alias: self._value = value

2. StubNode class (superset of all 5 per-file definitions):
   - __init__(self, name='Node', node_class='NoOp', xpos=0, ypos=0, knobs_dict=None)
     with self._set_name_calls = [] in addition to standard attributes
   - name(), fullName(), Class(), xpos(), ypos(), knobs()
   - screenWidth() returning 100
   - screenHeight() returning 50
   - input(index) and setInput(index, node)
   - setXYpos(x, y)
   - setName(new_name) that sets self._name = new_name AND appends new_name to self._set_name_calls
   - addKnob(knob) that stores: self._knobs[knob.name()] = knob  [functional: store by name]
   - removeKnob(knob) as no-op pass
   - __getitem__(knob_name) raising KeyError if knob_name not in self._knobs
   - __setitem__(knob_name, value)

3. make_stub_nuke_module() function that builds and returns types.ModuleType('nuke') with:
   - StubNode and StubKnob as class references on the stub
   - root = MagicMock(return_value=root_obj) where root_obj.name.return_value = 'destScript.nk'
   - allNodes = MagicMock(return_value=[])
   - toNode = MagicMock(return_value=None)
   - createNode = MagicMock()
   - selectedNodes = MagicMock(return_value=[])
   - nodeCopy = MagicMock()
   - nodePaste = MagicMock(return_value=None)
   - exists = MagicMock(return_value=False)
   - delete = MagicMock()
   - INVISIBLE = 0
   - NUKE_VERSION_MAJOR = 16  (critical: forces PySide6 path in anchor.py; do NOT use 14)
   - PyScript_Knob = MagicMock()
   - String_Knob = MagicMock(side_effect=lambda name, *args: StubKnob())
   - Tab_Knob = MagicMock(side_effect=lambda name, *args: StubKnob())
   - Boolean_Knob = MagicMock(side_effect=lambda name, *args: StubKnob())
   - zoom = MagicMock(return_value=1.0)
   - center = MagicMock(return_value=[0.0, 0.0])
   - zoomToFitSelected = MagicMock()
   - getColor = MagicMock(return_value=0)

4. make_stub_nukescripts_module() function that returns types.ModuleType('nukescripts') with:
   - cut_paste_file = lambda: '/tmp/nuke_stub_clipboard.nk'
   - clear_selection_recursive = MagicMock()

Imports needed at the top: import types; from unittest.mock import MagicMock

Do NOT import sys in stubs.py — it only defines classes and factory functions, it does
not install anything into sys.modules itself.
  </action>
  <verify>
    <automated>cd /workspace && python3 -c "from tests.stubs import StubKnob, StubNode, make_stub_nuke_module, make_stub_nukescripts_module; k=StubKnob(); k.setVisible(True); k.setFlag(1); k.setText('x'); n=StubNode(); n.screenWidth(); n.removeKnob(None); nuke=make_stub_nuke_module(); assert nuke.NUKE_VERSION_MAJOR==16; assert hasattr(nuke,'Tab_Knob'); ns=make_stub_nukescripts_module(); print('stubs.py OK')"</automated>
  </verify>
  <done>tests/stubs.py imports cleanly and all four exported names work without error. NUKE_VERSION_MAJOR is 16. Tab_Knob is present on the nuke stub.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create tests/conftest.py and update tests/__init__.py with authoritative stub installation</name>
  <files>tests/conftest.py, tests/__init__.py</files>
  <action>
Create /workspace/tests/conftest.py as a new file. This is the pytest-specific stub
installer — module-level code only, NO pytest fixtures.

conftest.py content structure:
1. Module docstring explaining it runs before any test file is collected by pytest
2. Imports: sys, types, MagicMock from unittest.mock
3. Import from stubs: make_stub_nuke_module, make_stub_nukescripts_module
4. PySide6 stub installation using MagicMock (NOT types.ModuleType) for all sub-modules:
   - _pyside6 = MagicMock(name='PySide6')
   - _pyside6.QtCore = MagicMock(name='PySide6.QtCore')
   - _pyside6.QtGui = MagicMock(name='PySide6.QtGui')
   - _pyside6.QtWidgets = MagicMock(name='PySide6.QtWidgets')
   - _pyside6.QtCore.Qt = MagicMock()
   - sys.modules['PySide6'] = _pyside6
   - sys.modules['PySide6.QtCore'] = _pyside6.QtCore
   - sys.modules['PySide6.QtGui'] = _pyside6.QtGui
   - sys.modules['PySide6.QtWidgets'] = _pyside6.QtWidgets
5. tabtabtab stub: types.ModuleType with TabTabTabPlugin=MagicMock, TabTabTabWidget=MagicMock
   - sys.modules['tabtabtab'] = _tabtabtab
6. nuke stub: sys.modules['nuke'] = make_stub_nuke_module()
7. nukescripts stub: sys.modules['nukescripts'] = make_stub_nukescripts_module()

Then update /workspace/tests/__init__.py (currently empty) to contain the SAME
stub installation logic. This is a separate code block, NOT importing from conftest —
__init__.py must be standalone so it works for `python3 -m unittest discover`.
Copy the same PySide6/tabtabtab/nuke/nukescripts installation pattern exactly.

The __init__.py addition ensures the test suite works under `python3 -m unittest discover`
(which does not load conftest.py) in addition to pytest.

IMPORTANT: In both conftest.py and __init__.py, the sys.modules assignments must be
at module level (not inside any function or if block). This guarantees execution at
import time, before any test module's own code runs.

IMPORTANT: In __init__.py, add an idempotency guard: only install if 'nuke' not already
in sys.modules. This prevents conftest.py and __init__.py from conflicting when pytest
imports __init__.py after conftest.py has already installed the stubs:

  if 'nuke' not in sys.modules:
      # install all stubs here
  </action>
  <verify>
    <automated>cd /workspace && python3 -c "import sys; sys.modules.pop('nuke', None); sys.modules.pop('tests', None); import tests; assert 'nuke' in sys.modules; assert sys.modules['nuke'].NUKE_VERSION_MAJOR == 16; assert 'PySide6' in sys.modules; print('__init__.py stub installation OK')"</automated>
  </verify>
  <done>tests/conftest.py exists with module-level stub installation using MagicMock for PySide6 sub-modules. tests/__init__.py installs stubs with idempotency guard. Both files import from stubs.py for nuke/nukescripts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Strip per-file stub blocks from five affected test files and verify zero errors</name>
  <files>tests/test_anchor_color_system.py, tests/test_anchor_navigation.py, tests/test_cross_script_paste.py, tests/test_dot_anchor_name_sync.py, tests/test_dot_type_distinction.py</files>
  <action>
Remove the per-file stub installation blocks from each of the five affected test files.
Each removal must be surgical — only the stub blocks go; all test code is preserved.

File-by-file instructions:

test_anchor_color_system.py:
  Remove the block that starts at the comment
  "# Stub Qt and tabtabtab modules" (line ~22) through the line
  sys.modules['nukescripts'].clear_selection_recursive = MagicMock() (~line 180).
  This includes: _make_stub_qt_module() function, all _pyside6_stub/tabtabtab_stub
  assignments, and the make_stub_nuke_module() function + its sys.modules assignments.
  DO NOT remove lines 182+ (the colors stub placeholder block starting with
  "# Stub colors module before importing anchor" — that sequence is test-file-specific
  and must remain). Also keep the _ensure_qt_stubs_support_mock_attributes-equivalent
  logic and all class definitions.

test_anchor_navigation.py:
  Remove the block from "# Stub Qt and tabtabtab modules" through
  sys.modules['nukescripts'] = _nukescripts_stub (~line 191).
  IMPORTANT: Do NOT remove the _ensure_qt_stubs_support_mock_attributes() function
  (it becomes a no-op once conftest provides MagicMock stubs, but callers in setUp()
  still reference it — removing it would break those setUp methods).

test_cross_script_paste.py:
  Remove the block from "# Stub Qt and tabtabtab modules" through
  sys.modules['nukescripts'].clear_selection_recursive = MagicMock() (~line 140).

test_dot_anchor_name_sync.py:
  Remove the block from "# Stub Qt and tabtabtab modules" through
  sys.modules['nukescripts'].clear_selection_recursive = MagicMock() (~line 170).

test_dot_type_distinction.py:
  Remove the block from "# Stub Qt and tabtabtab modules" through
  sys.modules['nukescripts'].clear_selection_recursive = MagicMock() (~line 173).

After removing all stub blocks, check each file still has: import sys, import types,
import unittest, from unittest.mock import MagicMock (keep all stdlib imports at the
top — only the stub installation code is removed, not the import lines themselves).

After all five files are modified, run the full test suite and confirm zero errors.
If any errors remain, diagnose by running each file in isolation:
  python3 -m unittest discover -s /workspace/tests -p "test_X.py"
and compare the error to the pitfalls in 08-RESEARCH.md.
  </action>
  <verify>
    <automated>cd /workspace && python3 -m unittest discover -s tests/ -p "test_*.py" 2>&1 | tail -5</automated>
  </verify>
  <done>python3 -m unittest discover -s tests/ runs 130 tests with 0 errors and 0 failures. Each individual test file also passes in isolation: python3 -m unittest discover -s tests/ -p "test_anchor_color_system.py" (etc.) all pass cleanly.</done>
</task>

</tasks>

<verification>
Overall phase gate commands:

1. Full suite under flat discovery (zero errors required):
   cd /workspace && python3 -m unittest discover -s tests/ -p "test_*.py"
   Expected: Ran 130 tests in ~0.4s, OK

2. Individual file isolation (each must pass alone):
   cd /workspace && python3 -m unittest tests.test_anchor_color_system
   cd /workspace && python3 -m unittest tests.test_anchor_navigation
   cd /workspace && python3 -m unittest tests.test_cross_script_paste
   cd /workspace && python3 -m unittest tests.test_dot_anchor_name_sync
   cd /workspace && python3 -m unittest tests.test_dot_type_distinction
   cd /workspace && python3 -m unittest tests.test_prefs

3. Verify no per-file stub blocks remain:
   grep -r "sys.modules\['PySide6'\]" /workspace/tests/test_*.py
   (should return no output)

4. Verify conftest.py and stubs.py exist:
   ls /workspace/tests/conftest.py /workspace/tests/stubs.py

Note on pytest: pytest is not installed in the current environment and network is
unreachable. The python3 -m unittest discover command is the working flat-discovery
equivalent for this machine. The conftest.py file is created per TEST-03 requirements
and will be the authoritative stub installer when pytest is available. The __init__.py
approach ensures tests pass now under unittest discover.
</verification>

<success_criteria>
1. python3 -m unittest discover -s tests/ runs with 0 errors, 0 failures (was 5 errors)
2. tests/conftest.py exists with module-level sys.modules stub installation
3. tests/stubs.py exists and exports all four symbols
4. tests/__init__.py installs stubs with idempotency guard
5. Zero occurrences of sys.modules['PySide6'] in any test_*.py file
6. test_prefs.py is unchanged
</success_criteria>

<output>
After completion, create .planning/phases/08-test-infrastructure-stabilization/08-01-SUMMARY.md
</output>
