---
phase: 07-color-picker-redesign-and-preferences-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - colors.py
autonomous: true
requirements:
  - PICKER-01
  - PICKER-02
  - PICKER-03
  - PICKER-04
  - PICKER-05

must_haves:
  truths:
    - "Clicking a swatch highlights it with a white border but does not close the dialog"
    - "Pressing Enter or clicking OK confirms the selected color and closes the dialog"
    - "The initial_color passed to the dialog opens with a white border highlight"
    - "Swatch groups appear in order: custom colors, then backdrop colors, then Nuke defaults"
    - "A blank row gap separates each non-empty group; empty groups are omitted"
    - "Custom Color... opens nuke.getColor(); the result is staged in memory, a new swatch is appended and auto-highlighted; the dialog stays open"
    - "Cancel discards any staged custom colors; the caller only receives them on accept"
  artifacts:
    - path: "colors.py"
      provides: "Redesigned ColorPaletteDialog with click-to-select, OK button, reordered groups, and custom-color staging"
      contains: "_refresh_swatch_borders"
    - path: "colors.py"
      provides: "chosen_custom_colors() accessor returning staged list"
      contains: "chosen_custom_colors"
  key_links:
    - from: "ColorPaletteDialog._on_swatch_clicked"
      to: "self._selected_color + _refresh_swatch_borders()"
      via: "removes self.accept() call; only updates state and border"
      pattern: "_refresh_swatch_borders"
    - from: "ColorPaletteDialog OK button"
      to: "self.accept()"
      via: "QPushButton.clicked signal; setAutoDefault(False) required"
      pattern: "setAutoDefault"
    - from: "ColorPaletteDialog._on_custom_color_clicked"
      to: "self._staged_custom_colors"
      via: "appends result without calling accept(); dialog stays open"
      pattern: "_staged_custom_colors"
---

<objective>
Redesign ColorPaletteDialog to use click-to-select (highlight-only) instead of click-to-close. Add an OK button for explicit confirmation. Reorder swatch groups to: custom colors, backdrop colors, Nuke defaults. Implement "Custom Color..." staging so colors added via nuke.getColor() are held in a dialog-local list and only returned to the caller on accept.

Purpose: PICKER-01 through PICKER-05 — the picker must behave as a deliberate selection dialog, not an immediate-fire picker.
Output: Modified colors.py with the redesigned ColorPaletteDialog class.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-color-picker-redesign-and-preferences-panel/07-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts from colors.py the executor will modify. -->

Current ColorPaletteDialog.__init__ signature (colors.py line 93):
```python
def __init__(self, initial_color=None, show_name_field=False,
             initial_name="", custom_colors=None, parent=None):
```
This signature must NOT change — callers in anchor.py pass custom_colors via constructor injection.

Current _on_swatch_clicked (colors.py lines 192-196) — THE LINE TO REMOVE:
```python
def _on_swatch_clicked(self, color_int):
    self._selected_color = color_int
    if self._name_edit is not None:
        self.chosen_name = self._name_edit.text()
    self.accept()    # <-- REMOVE THIS LINE in Phase 7
```

Current group ordering in _build_ui (colors.py line 132) — TO REVERSE:
```python
all_color_groups = [
    group for group in [nuke_pref_colors, backdrop_colors, user_palette_colors]
    if group
]
# Phase 7: reverse to [user_palette_colors, backdrop_colors, nuke_pref_colors]
```

Current keyPressEvent Enter block (colors.py lines 245-252) — CURRENTLY hint-mode only, must move OUTSIDE hint guard:
```python
if event.key() in (Qt.Key_Return, Qt.Key_Enter):
    if self._selected_color is not None:
        if self._name_edit is not None:
            self.chosen_name = self._name_edit.text()
        self.accept()
    event.accept()
    return
# Currently sits inside `if self._hint_mode:` — must be extracted above that block
```

_color_int_to_rgb utility (colors.py line 58):
```python
def _color_int_to_rgb(color_int):
    """Unpack a 0xRRGGBBAA int into (r, g, b) tuple."""
    red = (color_int >> 24) & 0xFF
    green = (color_int >> 16) & 0xFF
    blue = (color_int >> 8) & 0xFF
    return red, green, blue
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Click-to-select, OK button, group reordering</name>
  <files>colors.py, tests/test_anchor_color_system.py</files>
  <behavior>
    - Clicking a swatch calls _on_swatch_clicked which updates self._selected_color and calls _refresh_swatch_borders() — does NOT call self.accept()
    - _refresh_swatch_borders() iterates self._swatch_cells; selected swatch gets "border: 2px solid white"; others get "border: 1px solid #555"
    - The OK button (QPushButton, setAutoDefault(False)) is wired to self.accept()
    - Cancel button unchanged (already wired to self.reject())
    - swatch groups ordered [user_palette_colors, backdrop_colors, nuke_pref_colors]
    - Enter key (Qt.Key_Return, Qt.Key_Enter) calls accept() regardless of hint mode — the Enter block moves OUTSIDE the `if self._hint_mode:` guard
    - Pre-selected initial_color shows white border on open (already handled in _build_ui; preserved through refactor)
    - All QPushButton instances (OK, Cancel, Custom Color..., swatch buttons) call setAutoDefault(False)
  </behavior>
  <action>
    Modify ColorPaletteDialog in colors.py:

    1. In `_on_swatch_clicked`: remove the `self.accept()` call and the `chosen_name` assignment. The method becomes: set `self._selected_color = color_int`, then call `self._refresh_swatch_borders()`.

    2. Add new method `_refresh_swatch_borders(self)`: iterate `self._swatch_cells` (list of (col_index, row_index, color_int, button) tuples). For each entry, call `_color_int_to_rgb(color_int)` and apply the white-border stylesheet if `color_int == self._selected_color`, otherwise the default-border stylesheet. Use `if self._selected_color is not None and color_int == self._selected_color` — never use falsy test since 0 is valid.

    3. In `_build_ui`: reverse the group list order from `[nuke_pref_colors, backdrop_colors, user_palette_colors]` to `[user_palette_colors, backdrop_colors, nuke_pref_colors]`. The existing blank-row gap logic (`if group_col > 0: grid_row += 1` at end of each color_group loop) already produces the visual separation — no structural change needed, just reorder.

    4. In `_build_ui`: add an OK button after the swatch grid, BEFORE the Cancel button. `ok_button = QtWidgets.QPushButton("OK")`. Call `ok_button.setAutoDefault(False)`. Call `ok_button.setFocusPolicy(Qt.NoFocus)`. Wire to `self.accept()`. Add to `outer_layout` before the Cancel button. Also call `cancel_button.setAutoDefault(False)` on the existing Cancel button. Also call `custom_button.setAutoDefault(False)` on the existing Custom Color button.

    5. In `keyPressEvent`: move the Enter-key block OUTSIDE the `if self._hint_mode:` guard. The block currently lives at lines 245-252 inside hint mode — extract it to run unconditionally (before the `if self._hint_mode:` check). The hint-mode letter/row key handling remains inside the guard. The structure becomes:
       ```
       if Key_Escape → reject
       if Key_Tab → toggle hint mode
       if Key_Return or Key_Enter:
           if self._selected_color is not None:
               if self._name_edit is not None: self.chosen_name = ...
               self.accept()
           event.accept(); return
       if self._hint_mode:
           [column key handling]
           [row key handling]
           event.accept(); return
       super().keyPressEvent(event)
       ```

    6. Add `setAutoDefault(False)` to each swatch QPushButton in the `for color_int in color_group:` loop (same loop where buttons are created).

    Write tests in tests/test_anchor_color_system.py covering: (a) _on_swatch_clicked updates _selected_color without closing, (b) _refresh_swatch_borders applies correct stylesheet to selected vs non-selected swatches.
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/test_anchor_color_system.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>_on_swatch_clicked no longer calls accept(); _refresh_swatch_borders exists and styles swatches correctly; OK button present; Enter key works outside hint mode; group order is custom→backdrop→nuke; all buttons have setAutoDefault(False). Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Custom Color staging in memory (PICKER-05)</name>
  <files>colors.py, tests/test_anchor_color_system.py</files>
  <behavior>
    - __init__ initializes self._staged_custom_colors as a copy of custom_colors param (list(custom_colors) if custom_colors else [])
    - _on_custom_color_clicked: if nuke.getColor() returns non-zero, append to self._staged_custom_colors, set self._selected_color = result, call self._append_swatch_to_custom_group(result), call _refresh_swatch_borders(), do NOT call self.accept()
    - _append_swatch_to_custom_group(color_int): creates a new swatch button and adds it to the first group's row in the grid; updates self._swatch_cells and self._cell_map
    - chosen_custom_colors() returns list(self._staged_custom_colors)
    - If nuke.getColor() returns 0, _on_custom_color_clicked returns early with no state change
    - On cancel (reject), staged colors are not returned to caller (caller checks dialog.result())
  </behavior>
  <action>
    Modify ColorPaletteDialog in colors.py:

    1. In `__init__`: add `self._staged_custom_colors = list(custom_colors) if custom_colors else []` after `self._custom_colors = ...` assignment. This is the local working copy used for staging newly added colors.

    2. Redesign `_on_custom_color_clicked`: change it to append to `self._staged_custom_colors` (not `self._custom_colors`), call `self._selected_color = result`, then call a new helper `self._append_swatch_to_custom_group(result)`, then call `self._refresh_swatch_borders()`. Remove the `chosen_name` assignment and remove the `self.accept()` call. The dialog stays open.

    3. Add helper `_append_swatch_to_custom_group(self, color_int)`: create a new QPushButton (24×24, NoFocus, setAutoDefault(False)). Apply the default swatch stylesheet. Wire `button.clicked.connect(lambda checked=False, c=color_int: self._on_swatch_clicked(c))`. Determine the grid position: the custom colors group always occupies the first rows of the grid (they appear first after PICKER-04 reordering). Track the current last occupied position in the custom group. The simplest approach: store `self._custom_group_next_col` and `self._custom_group_next_row` counters (initialized in `_build_ui` after populating the custom group, tracking the next available slot). Add the new button at `(self._custom_group_next_row, self._custom_group_next_col)` using `self._grid_layout.addWidget(button, self._custom_group_next_row, self._custom_group_next_col)`. Append `(self._custom_group_next_col, self._custom_group_next_row, color_int, button)` to `self._swatch_cells`. Advance the column counter; wrap to next row at `_SWATCHES_PER_ROW`.

    4. Add public accessor `chosen_custom_colors(self)`: returns `list(self._staged_custom_colors)`.

    5. In `_build_ui`: after the custom colors group loop finishes (inside the `for color_group in all_color_groups:` loop, specifically for the first group which is user_palette_colors), initialize `self._custom_group_next_col = group_col` and `self._custom_group_next_row = grid_row` to record the next empty slot for dynamic appending. If the custom colors group is empty (no custom_colors were provided), initialize both to 0 so appended swatches start at row 0.

    Add tests covering: staged_custom_colors is separate from custom_colors; chosen_custom_colors() returns the staged list; nuke.getColor() returning 0 causes no state change.
  </action>
  <verify>
    <automated>cd /workspace && python -m pytest tests/test_anchor_color_system.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>_on_custom_color_clicked stages colors without closing dialog; chosen_custom_colors() returns staged list; getColor() == 0 is a no-op; tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite: `cd /workspace && python -m pytest tests/ -x -q 2>&1 | tail -30`

Verify colors.py is syntactically valid: `cd /workspace && python -c "import ast; ast.parse(open('colors.py').read()); print('OK')" 2>&1`
</verification>

<success_criteria>
- ColorPaletteDialog._on_swatch_clicked does not call accept()
- _refresh_swatch_borders() method exists and applies white border to selected swatch only
- OK button present beside Cancel; both have setAutoDefault(False)
- Enter key works outside hint mode
- Group order is custom colors → backdrop colors → Nuke defaults
- chosen_custom_colors() returns staged list (independent of cancel/accept state)
- All test_anchor_color_system.py tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-color-picker-redesign-and-preferences-panel/07-01-SUMMARY.md`
</output>
