---
phase: 07-color-picker-redesign-and-preferences-panel
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - menu.py
autonomous: false
requirements:
  - PANEL-01

must_haves:
  truths:
    - "Anchors menu contains a Preferences... item"
    - "Preferences... is always active regardless of plugin_enabled state"
    - "Clicking Preferences... opens the PrefsDialog"
    - "Preferences... item is visually separated from the Copy/Cut/Paste (old) fallback commands"
  artifacts:
    - path: "menu.py"
      provides: "Preferences... command registered on anchors_menu"
      contains: "Preferences..."
  key_links:
    - from: "Anchors menu Preferences... item"
      to: "PrefsDialog in colors.py"
      via: "addCommand callback string: from colors import PrefsDialog; dlg = PrefsDialog(); dlg.exec_()"
      pattern: "PrefsDialog"
---

<objective>
Wire the Preferences menu entry into the Anchors menu in menu.py, and verify the full Phase 7 feature set works end-to-end.

Purpose: PANEL-01 — the entry point users need to reach PrefsDialog.
Output: menu.py updated with Preferences... command; human verification of full flow.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-color-picker-redesign-and-preferences-panel/07-02-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from menu.py for the executor. -->

menu.py always-active block (lines 67-69):
```python
anchors_menu.addCommand("Copy (old)",  "paste_hidden.copy_old()")
anchors_menu.addCommand("Cut (old)",   "paste_hidden.cut_old()")
anchors_menu.addCommand("Paste (old)", "paste_hidden.paste_old()", "+^D")
```

menu.py _gated_menu_items list (line 33):
```python
_gated_menu_items = []
```
The Preferences... command must NOT be added to _gated_menu_items — it must remain always active.

menu.py set_anchors_menu_enabled (lines 72-80):
```python
def set_anchors_menu_enabled(enabled):
    for menu_item in _gated_menu_items:
        if hasattr(menu_item, 'setEnabled'):
            menu_item.setEnabled(enabled)
```
Only items in _gated_menu_items are toggled — items registered with plain addCommand (not _add_gated_command) are always active. Preferences must use plain addCommand.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Preferences... command to Anchors menu</name>
  <files>menu.py</files>
  <action>
    In menu.py, after the three always-active Copy(old)/Cut(old)/Paste(old) commands, add:

    ```python
    anchors_menu.addSeparator()
    anchors_menu.addCommand(
        "Preferences...",
        "from colors import PrefsDialog; dlg = PrefsDialog(); dlg.exec_()"
    )
    ```

    Use plain `anchors_menu.addCommand()` (NOT `_add_gated_command()`) so the item is NOT added to `_gated_menu_items`. This ensures it remains active when plugin_enabled is False — consistent with Phase 06-04 decision that copy_old/cut_old/paste_old and Preferences are always reachable.

    The `addSeparator()` visually separates the fallback old-style commands from the Preferences entry. This matches the layout decision in RESEARCH.md Pattern 7.

    Verify menu.py syntax after the change.
  </action>
  <verify>
    <automated>cd /workspace && python -c "import ast; ast.parse(open('menu.py').read()); print('syntax OK')" 2>&1</automated>
  </verify>
  <done>menu.py contains addSeparator() and addCommand("Preferences...") using plain addCommand (not _add_gated_command); syntax valid.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of full Phase 7 feature set</name>
  <action>Human performs interactive UAT of all Phase 7 flows in Nuke — see how-to-verify below.</action>
  <verify>Human confirms "approved" after testing all 14 verification steps.</verify>
  <done>Human types "approved"; all color picker and PrefsDialog flows verified in Nuke.</done>
  <what-built>
    Phase 7 complete: ColorPaletteDialog redesigned (click-to-select, OK button, group reordering, custom color staging) and PrefsDialog added (plugin toggle, LINK_CLASSES toggle, custom color CRUD). Preferences... entry added to Anchors menu.
  </what-built>
  <how-to-verify>
    Open Nuke and test the following flows:

    **Color Picker (requires an anchor node or use Anchors > Create Anchor):**
    1. Open the color picker (e.g. create an anchor, or use Rename Anchor). Confirm:
       - Clicking a swatch highlights it with a white border without closing the dialog
       - The previously set anchor color is pre-highlighted when the dialog opens
       - Pressing Enter confirms the selection and closes; the anchor color updates
       - Clicking OK confirms; clicking Cancel discards
       - Swatch group order is: custom colors (if any), backdrop colors, Nuke defaults
       - "Custom Color..." opens the Nuke color chooser; picking a color appends a new swatch and auto-selects it without closing; pressing Cancel on the picker does NOT add the color; pressing OK on the dialog persists the new custom color

    **Preferences Dialog:**
    2. Open Anchors menu — confirm "Preferences..." item is visible at the bottom with a separator above it
    3. Click Preferences... — dialog opens with two checkboxes and a swatch grid
    4. Verify "Enable paste_hidden plugin" is checked (default)
    5. Verify "Input nodes paste as links" is checked (default create_link)
    6. Toggle "Enable paste_hidden plugin" OFF → click OK → re-open Preferences — confirm it shows unchecked
    7. Restart Nuke (or re-run the plugin init) → open Preferences — confirm toggle state survived
    8. Toggle "Input nodes paste as links" OFF → OK → copy a Read node → confirm plain Nuke copy (no FQNN stamp)
    9. Add a custom color via Add button → confirm it appears in the swatch grid
    10. Select that swatch → click Edit → pick a different color → confirm it replaced in-place
    11. Select a swatch → click Remove → confirm it disappears from the grid
    12. Confirm Edit and Remove are greyed until a swatch is selected
    13. Click Cancel after making changes — confirm prefs are not changed

    **Anchors menu plugin_enabled gating:**
    14. With plugin disabled (from step 6/7), open Anchors menu — confirm gated items (Create Anchor, etc.) are greyed; Copy(old)/Cut(old)/Paste(old)/Preferences... remain active
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Run full test suite before human verification: `cd /workspace && python -m pytest tests/ -x -q 2>&1 | tail -30`
</verification>

<success_criteria>
- menu.py has Preferences... command using plain addCommand (ungated)
- Separator before Preferences... in Anchors menu
- Human verification approved: color picker click-to-select, Enter/OK confirmation, group ordering, custom color staging, PrefsDialog all controls, persistence across restart, menu gating
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-color-picker-redesign-and-preferences-panel/07-03-SUMMARY.md`
</output>
