---
phase: 06-preferences-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - colors.py
autonomous: true
requirements:
  - PREFS-01

must_haves:
  truths:
    - "colors.py no longer defines load_user_palette() or save_user_palette()"
    - "ColorPaletteDialog accepts a custom_colors parameter and uses it instead of calling load_user_palette()"
    - "colors.py does not import prefs anywhere"
    - "Callers that pass custom_colors=[...] see those colors rendered in the user palette row of the dialog"
  artifacts:
    - path: "colors.py"
      provides: "ColorPaletteDialog with custom_colors constructor parameter"
      contains: "custom_colors=None"
  key_links:
    - from: "ColorPaletteDialog.__init__"
      to: "_build_ui"
      via: "self._custom_colors stored on self; used in _build_ui as user_palette_colors"
      pattern: "self._custom_colors"
---

<objective>
Remove load_user_palette() and save_user_palette() from colors.py; add custom_colors constructor parameter to ColorPaletteDialog.

Purpose: Breaks the colors.py dependency on the old palette file and prevents a circular import (colors.py must never import prefs.py). Callers will inject custom_colors from prefs.custom_colors at call time.
Output: Modified colors.py — two functions removed, ColorPaletteDialog.__init__ gains custom_colors parameter.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-preferences-infrastructure/06-CONTEXT.md
@.planning/phases/06-preferences-infrastructure/06-RESEARCH.md
@.planning/phases/06-preferences-infrastructure/06-01-SUMMARY.md

<interfaces>
<!-- Current ColorPaletteDialog signature (colors.py line 116): -->
```python
def __init__(self, initial_color=None, show_name_field=False, initial_name="", parent=None):
```

<!-- Current _build_ui line that calls load_user_palette (colors.py line 151): -->
```python
user_palette_colors = load_user_palette()
```

<!-- USER_PALETTE_PATH import in colors.py (line 21): -->
```python
from constants import USER_PALETTE_PATH
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove palette functions and add custom_colors parameter</name>
  <files>colors.py</files>
  <action>
Make these targeted changes to colors.py:

**1. Remove `load_user_palette()` function** (lines 28-38):
Delete the entire `load_user_palette()` function including its docstring.

**2. Remove `save_user_palette()` function** (lines 41-45):
Delete the entire `save_user_palette()` function including its docstring.

**3. Remove `USER_PALETTE_PATH` from the import** (line 21):
Change:
```python
from constants import USER_PALETTE_PATH
```
To remove it entirely — colors.py no longer needs any constant from constants.py at the top level (the Qt guard below uses nuke directly). If constants.py imports are empty after removal, remove the entire import line.

**4. Update `ColorPaletteDialog.__init__` signature** — add `custom_colors=None` parameter before `parent=None`:
```python
def __init__(self, initial_color=None, show_name_field=False,
             initial_name="", custom_colors=None, parent=None):
```

**5. Store `custom_colors` on `self` in `__init__`, before `_build_ui` call**:
```python
self._custom_colors = custom_colors if custom_colors is not None else []
```

**6. In `_build_ui()`, replace the `load_user_palette()` call** (line 151):
Change:
```python
user_palette_colors = load_user_palette()
```
To:
```python
user_palette_colors = self._custom_colors
```

**Do NOT change anything else** — the ordering of color groups (nuke_pref_colors, backdrop_colors, user_palette_colors) stays identical. The dialog layout, hint mode, keyboard handling — all unchanged.

**Circular import prevention:** colors.py must NOT gain any `import prefs` line. The custom_colors value flows in via the constructor — callers (anchor.py in Plan 04) will pass `prefs.custom_colors`.
  </action>
  <verify>
    <automated>python3 -c "
import sys
sys.path.insert(0, '/workspace')

# Verify removed functions are gone
import colors
assert not hasattr(colors, 'load_user_palette'), 'load_user_palette should be removed'
assert not hasattr(colors, 'save_user_palette'), 'save_user_palette should be removed'

# Verify no prefs import
import inspect
source = inspect.getsource(colors)
assert 'import prefs' not in source, 'colors.py must not import prefs'
assert 'USER_PALETTE_PATH' not in source, 'USER_PALETTE_PATH should be removed from colors.py'

print('colors.py transition OK')
"
    </automated>
  </verify>
  <done>
  - load_user_palette() and save_user_palette() removed from colors.py
  - ColorPaletteDialog.__init__ accepts custom_colors parameter
  - _build_ui() reads self._custom_colors instead of calling load_user_palette()
  - No import of prefs in colors.py
  - USER_PALETTE_PATH import removed from colors.py
  </done>
</task>

</tasks>

<verification>
After task completes:

1. `python3 -c "import colors; print('OK')"` — imports cleanly with no error
2. `grep -n "load_user_palette\|save_user_palette\|import prefs\|USER_PALETTE_PATH" /workspace/colors.py` — returns no matches
3. `grep -n "custom_colors" /workspace/colors.py` — shows the parameter in __init__ and the self._custom_colors assignment and usage in _build_ui
</verification>

<success_criteria>
- colors.py imports cleanly (python3 -c "import colors")
- load_user_palette and save_user_palette are absent
- ColorPaletteDialog constructor signature includes custom_colors=None
- No circular import risk: colors.py never imports prefs
</success_criteria>

<output>
After completion, create `.planning/phases/06-preferences-infrastructure/06-02-SUMMARY.md`
</output>
