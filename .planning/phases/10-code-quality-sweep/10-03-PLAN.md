---
phase: 10-code-quality-sweep
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - paste_hidden.py
  - menu.py
  - constants.py
  - prefs.py
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "ruff check reports zero violations across all workspace-root .py source files"
    - "constants.py has a FROZEN annotation comment above every serialized knob name constant"
    - "No serialized knob name constant value has been renamed or altered"
    - "paste_hidden.py copy_hidden and paste_hidden functions have noqa: C901 with explanatory comment"
    - "Full test suite passes green including BUG-01 and BUG-02 regression tests"
    - "menu.py F401 violations are suppressed by pyproject.toml per-file-ignores (not inline noqa)"
  artifacts:
    - path: "paste_hidden.py"
      provides: "zero ruff violations — E501 fixed, C901 suppressed with noqa comment on def lines"
      contains: "# noqa: C901"
    - path: "menu.py"
      provides: "zero ruff violations — E501 fixed; F401 already handled by pyproject.toml per-file-ignores"
    - path: "constants.py"
      provides: "FROZEN annotation comments above all 8 serialized knob name constants; E501 fixed"
      contains: "# FROZEN: value stored in .nk files"
    - path: "prefs.py"
      provides: "zero ruff violations — E501 fixed"
  key_links:
    - from: "constants.py KNOB_NAME"
      to: ".nk files on disk"
      via: "Nuke knob serialization"
      pattern: "# FROZEN: value stored in .nk files — do not rename"
    - from: "paste_hidden.py paste_hidden()"
      to: "tests/test_cross_script_paste.py"
      via: "BUG-01 and BUG-02 regression tests"
      pattern: "test_cross_script_paste"
---

<objective>
Fix remaining ruff violations in paste_hidden.py, menu.py, constants.py, and prefs.py; add FROZEN annotations to constants.py.

Purpose: Complete the sweep. paste_hidden.py requires care — BUG-01/BUG-02 fixes are recent; E501 wrapping is safe but C901 must be suppressed with noqa rather than structurally refactored. FROZEN annotations in constants.py fulfill the success criterion that serialized knob names are explicitly documented.
Output: All workspace-root source files at zero ruff violations; FROZEN annotations present; test suite green.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-code-quality-sweep/10-RESEARCH.md
@.planning/phases/10-code-quality-sweep/10-02-SUMMARY.md

<interfaces>
<!-- Ruff binary -->
/home/latuser/.local/share/nvim/mason/bin/ruff

<!-- Test commands -->
python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py"
python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_cross_script_paste.py"

<!-- FROZEN constants in constants.py (all 8 must be annotated) -->
KNOB_NAME                  = 'copy_hidden_input_node'
TAB_NAME                   = 'copy_hidden_tab'
LINK_RECONNECT_KNOB_NAME   = 'reconnect_link'
ANCHOR_RECONNECT_KNOB_NAME = 'reconnect_child_links'
ANCHOR_RENAME_KNOB_NAME    = 'rename_anchor'
DOT_ANCHOR_KNOB_NAME       = 'paste_hidden_dot_anchor'
DOT_TYPE_KNOB_NAME         = 'paste_hidden_dot_type'
ANCHOR_SET_COLOR_KNOB_NAME = 'set_anchor_color'

Annotation pattern (add as comment on the line immediately above each constant):
  # FROZEN: value stored in .nk files — do not rename
  KNOB_NAME = 'copy_hidden_input_node'

CRITICAL: Only add comment lines. Do NOT rename, reorder, or alter any constant value.

<!-- paste_hidden.py C901 suppression pattern -->
The two functions at complexity 14 (threshold 10) are copy_hidden() and paste_hidden().
Suppression pattern (add noqa comment to the def line):
  def copy_hidden(cut=False):  # noqa: C901 — complexity is inherent: 3 node-class paths × same/cross-script gate
  def paste_hidden():  # noqa: C901 — complexity is inherent: anchor/link/dot paths × same/cross-script gate

Do NOT extract helper functions from copy_hidden or paste_hidden to reduce complexity.
Reason: paste_hidden() mutates selected_nodes in-place (.remove(), .append()); extraction would
require passing the list as a parameter. BUG-01/BUG-02 fixes are recent; structural refactoring
risks regressing them. The # noqa approach is the documented safe decision.

<!-- menu.py F401 already handled -->
menu.py F401 violations (paste_hidden, anchor, labels imports) are suppressed by the
pyproject.toml per-file-ignores added in Plan 01. Do NOT add inline # noqa comments to
menu.py for F401. Only fix the remaining E501 violations in menu.py.

<!-- paste_hidden.py E501 note -->
paste_hidden.py had 15 E501 violations at line-length=88. At line-length=100, approximately
4-7 remain. These are safe to fix: wrap inline comments, split long if-conditions across
lines using continuation or parentheses, extract long string arguments to a variable.
NEVER alter the logic or function signatures while fixing line length.
After EVERY edit to paste_hidden.py run:
  python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_cross_script_paste.py"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix paste_hidden.py E501 and suppress C901; fix menu.py and prefs.py E501</name>
  <files>paste_hidden.py, menu.py, prefs.py</files>
  <action>
Read each file fully before editing.

**paste_hidden.py:**
1. Run `ruff check /workspace/paste_hidden.py` to get the current violation list with exact line numbers.
2. E501 fixes: For each long line, wrap it. Safe strategies:
   - Long inline comment: move comment to line above or split across lines
   - Long if-condition: wrap with parentheses and split across lines (no logic change)
   - Long function call with many args: align args on continuation lines
   - Long string literal in a comment: split the comment
   NEVER change any logic, branching, or variable mutation.
3. C901 (2x — copy_hidden and paste_hidden): Add noqa comment to each `def` line:
   ```python
   def copy_hidden(cut=False):  # noqa: C901 — complexity is inherent: 3 node-class paths × same/cross-script gate
   def paste_hidden():  # noqa: C901 — complexity is inherent: anchor/link/dot paths × same/cross-script gate
   ```
   Do NOT change anything else about these functions.
4. After EVERY edit to paste_hidden.py, run the targeted regression test immediately:
   ```bash
   python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_cross_script_paste.py" 2>&1 | tail -5
   ```
5. After all paste_hidden.py edits complete, confirm zero violations:
   ```bash
   /home/latuser/.local/share/nvim/mason/bin/ruff check /workspace/paste_hidden.py
   ```

**menu.py:**
1. Run `ruff check /workspace/menu.py` to get remaining violations (F401 should already be suppressed by pyproject.toml).
2. E501 only: Fix remaining long lines by wrapping inline comments or splitting long string arguments in addCommand() calls. Do not alter the string callback values (e.g., `"paste_hidden.copy_hidden()"`) — these are executed by Nuke's eval.
3. Confirm zero violations: `ruff check /workspace/menu.py`

**prefs.py:**
1. Run `ruff check /workspace/prefs.py` to get the single remaining E501 violation with its line number.
2. Fix the long line (wrap comment or split expression).
3. Confirm zero violations: `ruff check /workspace/prefs.py`

After all three files, run the full test suite:
```bash
python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py" 2>&1 | tail -3
```
  </action>
  <verify>
    <automated>/home/latuser/.local/share/nvim/mason/bin/ruff check /workspace/paste_hidden.py /workspace/menu.py /workspace/prefs.py 2>&1 && python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py" 2>&1 | tail -3</automated>
  </verify>
  <done>Zero ruff violations on paste_hidden.py, menu.py, prefs.py; full test suite green including BUG-01/BUG-02 regression tests</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add FROZEN annotations to constants.py; fix constants.py E501; verify global zero violations</name>
  <files>constants.py</files>
  <action>
Read constants.py fully before editing.

**constants.py FROZEN annotations:**
Add the comment `# FROZEN: value stored in .nk files — do not rename` on the line immediately above each of these 8 constants:
- KNOB_NAME
- TAB_NAME
- LINK_RECONNECT_KNOB_NAME
- ANCHOR_RECONNECT_KNOB_NAME
- ANCHOR_RENAME_KNOB_NAME
- DOT_ANCHOR_KNOB_NAME
- DOT_TYPE_KNOB_NAME
- ANCHOR_SET_COLOR_KNOB_NAME

Do NOT annotate non-serialized constants (e.g., display names, color values, UI labels).
Do NOT rename, reorder, or change any constant values. Only add comment lines.

**constants.py E501:**
Fix the one long line (at line-length=100). It is likely a comment. Wrap it.

**Global zero-violation check:**
After constants.py is complete, run ruff against ALL workspace-root source files to confirm the phase success criterion is met:
```bash
/home/latuser/.local/share/nvim/mason/bin/ruff check /workspace/anchor.py /workspace/colors.py /workspace/constants.py /workspace/labels.py /workspace/link.py /workspace/menu.py /workspace/paste_hidden.py /workspace/prefs.py /workspace/tabtabtab.py /workspace/util.py
```
Expected output: zero violations across all files.

Then run the full test suite one final time:
```bash
python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py"
```
Expected: all 132 tests pass, 0 errors, 0 failures.
  </action>
  <verify>
    <automated>/home/latuser/.local/share/nvim/mason/bin/ruff check /workspace/anchor.py /workspace/colors.py /workspace/constants.py /workspace/labels.py /workspace/link.py /workspace/menu.py /workspace/paste_hidden.py /workspace/prefs.py /workspace/tabtabtab.py /workspace/util.py 2>&1 && python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py" 2>&1 | tail -3</automated>
  </verify>
  <done>Zero ruff violations across all 10 source files; FROZEN annotations present above all 8 serialized knob name constants; full test suite green</done>
</task>

</tasks>

<verification>
Phase 10 completion gate:
1. `ruff check` on all workspace-root .py files returns zero violations
2. `grep -c "FROZEN" /workspace/constants.py` returns 8 (one annotation per serialized constant)
3. `python3 -m unittest discover -s tests/ -t . -p "test_*.py"` passes (132 tests, 0 errors, 0 failures)
4. No constant values in constants.py have been changed (only comment lines added)
5. No public function signatures have changed
</verification>

<success_criteria>
- Zero ruff violations across all 10 workspace-root Python source files
- All 8 serialized knob name constants carry FROZEN annotation comments
- Full test suite green (132 tests including BUG-01 and BUG-02 regression tests)
- No API breaks, no serialized constant renames
</success_criteria>

<output>
After completion, create `.planning/phases/10-code-quality-sweep/10-03-SUMMARY.md` with:
- Final ruff violation count (must be 0)
- List of FROZEN constants annotated
- Confirmation of C901 noqa decision for copy_hidden and paste_hidden
- Any violations that required unusual treatment
</output>
