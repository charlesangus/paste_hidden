---
phase: 10-code-quality-sweep
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - anchor.py
  - link.py
  - colors.py
  - labels.py
autonomous: true
requirements:
  - QUAL-01

must_haves:
  truths:
    - "anchor.py, link.py, colors.py, labels.py report zero ruff violations"
    - "link.py uses contextlib.suppress instead of bare try-except-pass for removeKnob calls"
    - "colors.py loop variables not used in the loop body are renamed with _ prefix"
    - "Full test suite passes green after all changes in this plan"
  artifacts:
    - path: "anchor.py"
      provides: "zero ruff violations — F401 removed or noqa'd, SIM105 replaced, SIM108 replaced, E501 fixed"
    - path: "link.py"
      provides: "zero ruff violations — SIM105 replaced with contextlib.suppress, F401 cleaned, E501 fixed"
    - path: "colors.py"
      provides: "zero ruff violations — B007 loop vars prefixed, E501 fixed"
    - path: "labels.py"
      provides: "zero ruff violations — E501 fixed"
  key_links:
    - from: "link.py add_input_knob()"
      to: "contextlib.suppress"
      via: "import contextlib at top of file"
      pattern: "import contextlib"
---

<objective>
Fix all ruff violations in anchor.py, link.py, colors.py, and labels.py.

Purpose: These four files carry the bulk of non-E501 violations (F401, SIM105, SIM108, B007) plus their E501 violations. None of them touch serialized knob data or the complex paste logic. They are the safest batch to fix after the import-sort baseline.
Output: Four source files at zero ruff violations; full test suite still green.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-code-quality-sweep/10-RESEARCH.md
@.planning/phases/10-code-quality-sweep/10-01-SUMMARY.md

<interfaces>
<!-- Ruff binary -->
/home/latuser/.local/share/nvim/mason/bin/ruff

<!-- Test command -->
python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py"

<!-- Per-file violation details (post Plan 01 I001 fixes, at line-length=100) -->

anchor.py remaining violations:
  - F401: 1 unused import (identify and remove or noqa if side-effect import)
  - SIM105: 3x try-except-pass blocks → replace with contextlib.suppress(Exception)
  - SIM108: 1x ternary simplification (if/else → x if cond else y)
  - E501: approximately 2-3 remaining lines above 100 chars (down from 9 at line-length=88)

link.py remaining violations:
  - F401: 2 unused imports (identify and remove — NOT the ones used in string callbacks; link.py has no Nuke menu registrations)
  - SIM105: 3x try-except-pass in add_input_knob() for node.removeKnob() calls
  - E501: approximately 1-2 remaining lines above 100 chars

colors.py remaining violations:
  - B007: 4 loop variables not used directly in loop body — rename to _ prefix
    NOTE: Before renaming, verify each variable is not captured in a lambda or inner function.
    All 4 are in swatch rendering loops per RESEARCH.md.
  - C901: 2 functions (complexity threshold exceeded) — check if these need noqa; do NOT
    attempt complexity refactoring in this plan (deferred to Plan 03 if applicable)
  - E501: approximately 5-10 remaining lines above 100 chars

labels.py remaining violations:
  - E501: approximately 1 remaining line above 100 chars

<!-- SIM105 replacement pattern for link.py -->
Before:
  try:
      node.removeKnob(node[DOT_TYPE_KNOB_NAME])
  except Exception:
      pass

After:
  with contextlib.suppress(Exception):
      node.removeKnob(node[DOT_TYPE_KNOB_NAME])

Add `import contextlib` to link.py's import block (ruff --fix --select I will have already sorted imports; insert contextlib in the stdlib block).

<!-- SIM108 replacement pattern for anchor.py -->
Before (example):
  if condition:
      value = a
  else:
      value = b

After:
  value = a if condition else b

Run ruff check anchor.py to identify the exact SIM108 line before editing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix anchor.py and link.py violations (F401, SIM105, SIM108, E501)</name>
  <files>anchor.py, link.py</files>
  <action>
Read each file fully before editing. Then fix violations in this order for each file:

**anchor.py:**
1. Run `ruff check /workspace/anchor.py` to get the current violation list with line numbers.
2. F401 (unused import): Identify which import is unused. If the import has no side-effect use anywhere in the file, remove the import line. If there is any doubt, add `# noqa: F401` with a comment explaining why.
3. SIM105 (3x): Replace each try-except-pass block with `with contextlib.suppress(Exception):`. Add `import contextlib` to the stdlib imports section at the top of anchor.py.
4. SIM108 (1x): Find the if/else block ruff identifies and collapse it to a ternary. Run ruff to confirm line number before editing.
5. E501: Fix remaining lines over 100 chars by wrapping inline comments to the next line or splitting long argument lists. Do NOT change string literals that are stored in .nk files (check RESEARCH.md constants table if any constant values appear inline).

**link.py:**
1. Run `ruff check /workspace/link.py` to get current violation list with line numbers.
2. F401 (2x): Remove the two unused imports. Confirm neither is a side-effect import by checking all usages in the file. `link.py` has no Nuke menu string callbacks, so all F401 here are genuine.
3. SIM105 (3x): All three are in `add_input_knob()`. Replace each try-except-pass block with `with contextlib.suppress(Exception):`. Add `import contextlib` to the stdlib imports block.
4. E501: Fix remaining lines over 100 chars.

After both files, run the full test suite:
```bash
python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py"
```

Then confirm zero violations on both files:
```bash
/home/latuser/.local/share/nvim/mason/bin/ruff check /workspace/anchor.py /workspace/link.py
```
  </action>
  <verify>
    <automated>/home/latuser/.local/share/nvim/mason/bin/ruff check /workspace/anchor.py /workspace/link.py 2>&1 && python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py" 2>&1 | tail -3</automated>
  </verify>
  <done>ruff check reports zero violations on anchor.py and link.py; full test suite passes</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix colors.py and labels.py violations (B007, C901, E501)</name>
  <files>colors.py, labels.py</files>
  <action>
Read each file fully before editing.

**colors.py:**
1. Run `ruff check /workspace/colors.py` to get current violation list with line numbers.
2. B007 (4x): For each flagged loop variable, read the loop body carefully. Check whether the variable appears in any lambda expression, inner function definition, or list comprehension nested inside the loop. If the variable is truly unused in the loop body and not captured in any inner scope, rename it to `_` (or `_varname` if there are multiple unused vars in the same loop to avoid shadowing).
3. C901 (2x): These are complexity violations on two functions in colors.py. Check which functions ruff identifies. Do NOT attempt to reduce complexity by extraction — add `# noqa: C901  # complexity is inherent to Qt swatch rendering logic` on each function's `def` line. The research notes these are in the swatch rendering area.
4. E501: Fix remaining lines over 100 chars. colors.py had 20 E501 at line-length=88; with line-length=100 approximately 5-10 remain. For inline comments, wrap to the next line. For long chained method calls, extract an intermediate variable.

**labels.py:**
1. Run `ruff check /workspace/labels.py` to get current violation list with line numbers.
2. E501: Fix the one or two remaining long lines. labels.py is a small file — the violations are likely in comments or string formatting.

After both files, run the full test suite:
```bash
python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py"
```

Then confirm zero violations on both files:
```bash
/home/latuser/.local/share/nvim/mason/bin/ruff check /workspace/colors.py /workspace/labels.py
```
  </action>
  <verify>
    <automated>/home/latuser/.local/share/nvim/mason/bin/ruff check /workspace/colors.py /workspace/labels.py 2>&1 && python3 -m unittest discover -s /workspace/tests/ -t /workspace -p "test_*.py" 2>&1 | tail -3</automated>
  </verify>
  <done>ruff check reports zero violations on colors.py and labels.py; full test suite passes</done>
</task>

</tasks>

<verification>
After Plan 02:
1. `ruff check /workspace/anchor.py /workspace/link.py /workspace/colors.py /workspace/labels.py` reports zero violations
2. `python3 -m unittest discover -s tests/ -t . -p "test_*.py"` passes (132 tests)
3. link.py uses `contextlib.suppress(Exception)` in `add_input_knob()` for the three removeKnob calls
4. colors.py B007 loop variables renamed; C901 functions have `# noqa: C901` with comment
</verification>

<success_criteria>
- Zero ruff violations on anchor.py, link.py, colors.py, labels.py
- Full test suite green
- No public API changes introduced
</success_criteria>

<output>
After completion, create `.planning/phases/10-code-quality-sweep/10-02-SUMMARY.md` with:
- Violations fixed per file
- Any B007 variables that were NOT renamed (with reason)
- Any surprises (e.g., unexpected F401 that was actually used)
</output>
