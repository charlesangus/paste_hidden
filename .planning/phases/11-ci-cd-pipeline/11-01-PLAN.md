---
phase: 11-ci-cd-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yml
autonomous: false
requirements:
  - CI-01
  - CI-02

must_haves:
  truths:
    - "Pushing a v* tag to GitHub triggers the workflow and produces a GitHub Release"
    - "The GitHub Release ZIP contains exactly the 10 plugin source .py files under paste_hidden/"
    - "The workflow fails and does not publish a release if pytest tests/ fails"
    - "A locally unzipped copy of the ZIP yields paste_hidden/anchor.py ... paste_hidden/util.py and nothing else"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered release workflow"
      contains: "on: push: tags"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "pytest tests/"
      via: "run: step before ZIP build"
      pattern: "pytest tests/"
    - from: ".github/workflows/release.yml"
      to: "paste_hidden/ staging directory"
      via: "cp + zip -r step"
      pattern: "zip -r.*paste_hidden"
    - from: ".github/workflows/release.yml"
      to: "softprops/action-gh-release@v2"
      via: "Publish GitHub Release step"
      pattern: "softprops/action-gh-release"
---

<objective>
Create the GitHub Actions release workflow that gates releases on offline tests, packages the plugin into a correctly structured ZIP, and publishes a GitHub Release.

Purpose: Artists download the plugin as a ZIP from GitHub Releases and unzip it into ~/.nuke/ — the paste_hidden/ subdirectory structure must be present for `import paste_hidden` to resolve. The test gate ensures broken code never ships.
Output: .github/workflows/release.yml (single file, ~35 lines of YAML)
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ci-cd-pipeline/11-CONTEXT.md
@.planning/phases/11-ci-cd-pipeline/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/release.yml</name>
  <files>.github/workflows/release.yml</files>
  <action>
Create the directory .github/workflows/ and write release.yml with the following exact structure:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install pytest
        run: pip install pytest

      - name: Run offline tests
        run: pytest tests/

      - name: Build release ZIP
        run: |
          mkdir paste_hidden
          cp anchor.py colors.py constants.py labels.py link.py \
             menu.py paste_hidden.py prefs.py tabtabtab.py util.py \
             paste_hidden/
          zip -r "paste_hidden-${GITHUB_REF_NAME}.zip" paste_hidden/

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          generate_release_notes: true
          files: paste_hidden-${{ github.ref_name }}.zip
```

Key correctness requirements (per RESEARCH.md pitfalls):
- Use `$GITHUB_REF_NAME` (shell env var) inside `run:` blocks; use `${{ github.ref_name }}` (Actions expression) inside `with:` inputs — do NOT mix them.
- Stage files into a `paste_hidden/` subdirectory BEFORE zipping so the archive contains `paste_hidden/anchor.py` (not bare `anchor.py` at root). This is mandatory for `~/.nuke/paste_hidden/` to resolve correctly.
- The ZIP step runs AFTER `pytest tests/` — test failure halts the job before any ZIP or release steps execute.
- `permissions: contents: write` is required for softprops/action-gh-release to create releases (avoids 403 error).
- The explicit `cp` file list (10 files) is the CI-02 manifest. Do NOT use `*.py` or any glob — that could capture stubs or __init__.py inadvertently. The 10 files: anchor.py, colors.py, constants.py, labels.py, link.py, menu.py, paste_hidden.py, prefs.py, tabtabtab.py, util.py.
- No `pip install -e .` or other package installs needed — tests use stdlib + project source only (per CONTEXT.md).
  </action>
  <verify>
    <automated>
      cd /workspace

      # Confirm file exists
      test -f .github/workflows/release.yml

      # Confirm trigger pattern is correct
      grep -q "tags:" .github/workflows/release.yml
      grep -q "v\*" .github/workflows/release.yml

      # Confirm permissions block exists
      grep -q "contents: write" .github/workflows/release.yml

      # Confirm all 10 manifest files are in the cp command
      for f in anchor.py colors.py constants.py labels.py link.py menu.py paste_hidden.py prefs.py tabtabtab.py util.py; do
        grep -q "$f" .github/workflows/release.yml || echo "MISSING: $f"
      done

      # Confirm test step precedes ZIP step (ordering is the gate)
      python3 -c "
content = open('.github/workflows/release.yml').read()
test_pos = content.index('pytest tests/')
zip_pos = content.index('zip -r')
assert test_pos < zip_pos, 'ERROR: test step must come before ZIP step'
print('Step ordering: OK')
"

      # Confirm softprops action is referenced
      grep -q "softprops/action-gh-release@v2" .github/workflows/release.yml

      # Confirm existing test suite still passes (workflow file must not break anything)
      python3 -m pytest tests/ -q
    </automated>
  </verify>
  <done>
    .github/workflows/release.yml exists with correct trigger (v* tags), permissions block, 10-file cp manifest, test-before-zip step ordering, and softprops/action-gh-release@v2. pytest tests/ passes (130 tests, 0 failures).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live tag push verification</name>
  <files>none — live verification only, no file changes</files>
  <action>Push a v* tag to GitHub and verify the workflow run produces a correct GitHub Release with the expected ZIP artifact. This step cannot be automated locally — it requires a live GitHub Actions run.</action>
  <verify>
    <automated>MISSING — live GitHub Actions run required; cannot be automated locally. See how-to-verify below.</automated>
  </verify>
  <done>GitHub Release exists with a correctly structured ZIP (10 files under paste_hidden/) and the workflow run completed green.</done>
  <what-built>
    GitHub Actions workflow at .github/workflows/release.yml — triggers on v* tags, runs pytest, builds ZIP with paste_hidden/ subdirectory structure, publishes GitHub Release with auto-generated notes.
  </what-built>
  <how-to-verify>
    1. Push the workflow file to GitHub if not already on remote: `git push origin main`
    2. Create and push a test tag: `git tag v1.2.0 && git push origin v1.2.0`
       (Or use a pre-release tag like `v1.2.0-rc1` if you want to keep v1.2.0 clean for the real release)
    3. Visit the GitHub Actions tab — confirm the "Release" workflow run appears and goes green
    4. Visit the GitHub Releases page — confirm a release named `v1.2.0` (or your tag) was created with auto-generated notes
    5. Download the ZIP from the release. Run: `unzip -l paste_hidden-v1.2.0.zip`
       Expected: 10 entries all under `paste_hidden/` (e.g. `paste_hidden/anchor.py`, `paste_hidden/colors.py`, ...)
       NOT expected: `tests/`, `.planning/`, `__pycache__`, `pyproject.toml`, or bare `anchor.py` at root
    6. Optional smoke test: unzip into a temp dir and confirm directory structure is `<tempdir>/paste_hidden/*.py`

    If the workflow fails: check the Actions log. Common causes are permissions (confirm `permissions: contents: write` present) or a test failure (check pytest step output).
  </how-to-verify>
  <resume-signal>Type "approved" if the release was published with the correct ZIP structure, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
- `test -f .github/workflows/release.yml` passes
- `grep -q "pytest tests/" .github/workflows/release.yml` passes
- `grep -q "softprops/action-gh-release@v2" .github/workflows/release.yml` passes
- `python3 -m pytest tests/ -q` exits 0 (workflow must not break the existing suite)
- Live: tag push creates a GitHub Release with a ZIP whose contents match the 10-file manifest under `paste_hidden/`
</verification>

<success_criteria>
1. Pushing a v* tag to GitHub triggers the "Release" workflow and produces a GitHub Release
2. The ZIP release artifact contains exactly 10 .py files under paste_hidden/ — no dev artifacts
3. The workflow fails the release if pytest tests/ fails (test gate enforced by step ordering)
4. A locally unzipped copy confirms paste_hidden/anchor.py ... paste_hidden/util.py structure
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-cd-pipeline/11-01-SUMMARY.md` following the summary template.
</output>
