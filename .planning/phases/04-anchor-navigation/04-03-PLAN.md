---
phase: 04-anchor-navigation
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements:
  - NAV-03

must_haves:
  truths:
    - "REQUIREMENTS.md marks NAV-03 as not complete ([ ]) and documents it as deferred to v2"
    - "REQUIREMENTS.md traceability table shows NAV-03 status as 'Deferred (v2)' not 'Complete'"
    - "ROADMAP.md Phase 4 plans list notes NAV-03 as a stretch goal not shipped"
    - "No planning document claims NAV-03 (full history stack) was implemented"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement status for NAV-03"
      contains: "[ ] **NAV-03**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 plan list with accurate NAV-03 scope note"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "v2 Requirements section"
      via: "NAV-V2-01 already present; NAV-03 traceability row updated"
      pattern: "Deferred.*v2"
---

<objective>
Correct the NAV-03 planning document inconsistency introduced when Phase 4 execution marked NAV-03 complete in frontmatter despite explicitly deferring it as a stretch goal.

Purpose: The code correctly implements a single-slot back-position save (satisfying NAV-01 and NAV-02). NAV-03 (full browser-style forward/back history stack) was never implemented — the 04-02-SUMMARY body states this clearly. However, REQUIREMENTS.md marks NAV-03 as `[x]` complete and the traceability table says "Complete". This contradicts both the code and the summary. The fix aligns planning documents with the actual implementation and decision to defer.

Output: REQUIREMENTS.md with NAV-03 correctly marked as not complete and the traceability row updated to "Deferred (v2)". ROADMAP.md Phase 4 plan list with a note that NAV-03 was deferred.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct NAV-03 status in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Make three targeted edits to .planning/REQUIREMENTS.md:

1. In the "Anchor Navigation History" section, change the NAV-03 line from:
   `- [x] **NAV-03**: Full browser-style forward/back navigation history stack (stretch goal)`
   to:
   `- [ ] **NAV-03**: Full browser-style forward/back navigation history stack (stretch goal — deferred to v2)`

2. In the Traceability table, change the NAV-03 row from:
   `| NAV-03 | Phase 4 | Complete |`
   to:
   `| NAV-03 | Phase 4 | Deferred (v2) — single-slot back implemented (NAV-01/02); full stack not shipped |`

3. Update the coverage count at the bottom. The current line says:
   `- v1 requirements: 18 total`
   Change it to:
   `- v1 requirements: 17 complete, 1 deferred (NAV-03 → v2)`
   (The "Mapped to phases: 18" and "Unmapped: 0" lines can remain — NAV-03 is still mapped, just not complete.)

Do not change any other lines. The v2 section already contains `NAV-V2-01` which covers the deferred full history stack — no addition needed there.
  </action>
  <verify>
    <automated>grep -n "NAV-03" /workspace/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>NAV-03 shows `[ ]` (not `[x]`), its note reads "deferred to v2", and the traceability row says "Deferred (v2)" not "Complete".</done>
</task>

<task type="auto">
  <name>Task 2: Add NAV-03 scope note to ROADMAP.md Phase 4 plan list</name>
  <files>.planning/ROADMAP.md</files>
  <action>
In the Phase 4 plan list inside the "### Phase 4: Anchor Navigation" section, the current 04-02 entry reads:
   `- [x] 04-02-PLAN.md — Backdrop picker: navigate_to_backdrop(), get_items() extension, invoke() dispatch, guard update (FIND-01)`

Change it to:
   `- [x] 04-02-PLAN.md — Backdrop picker: navigate_to_backdrop(), get_items() extension, invoke() dispatch, guard update (FIND-01) [NAV-03 stretch goal deferred to v2 — single-slot back shipped only]`

Also update the Requirements line for Phase 4 in the Phase Details section from:
   `**Requirements**: NAV-01, NAV-02, NAV-03, FIND-01`
to:
   `**Requirements**: NAV-01, NAV-02, FIND-01 (NAV-03 deferred to v2)`

Do not change any other lines in ROADMAP.md.
  </action>
  <verify>
    <automated>grep -n "NAV-03" /workspace/.planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md Phase 4 content notes NAV-03 as deferred to v2; the requirements line no longer presents NAV-03 as an in-scope shipped requirement.</done>
</task>

</tasks>

<verification>
After both tasks complete:

```
grep -n "NAV-03" /workspace/.planning/REQUIREMENTS.md /workspace/.planning/ROADMAP.md
```

Expected output shows:
- REQUIREMENTS.md: `[ ] **NAV-03**` (not `[x]`), "deferred to v2" in the requirement line, "Deferred (v2)" in the traceability table
- ROADMAP.md: "NAV-03 deferred to v2" in the requirements line and plan list

No line in either file should say NAV-03 is "Complete" or mark it `[x]`.
</verification>

<success_criteria>
- REQUIREMENTS.md NAV-03 checkbox is `[ ]` (was `[x]`)
- REQUIREMENTS.md traceability table NAV-03 row reads "Deferred (v2)" not "Complete"
- ROADMAP.md Phase 4 requirements line excludes NAV-03 as a shipped requirement (notes it deferred)
- ROADMAP.md Phase 4 plan list notes NAV-03 stretch goal was not shipped
- No code files changed — this is docs-only
- `python3 -m unittest discover -s tests -p "*.py"` continues to produce the same result as before (no regressions — no code touched)
</success_criteria>

<output>
After completion, create `.planning/phases/04-anchor-navigation/04-03-SUMMARY.md` using the summary template.
</output>
