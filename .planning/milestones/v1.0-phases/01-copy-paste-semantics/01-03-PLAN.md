---
phase: 01-copy-paste-semantics
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - link.py
  - anchor.py
autonomous: true
gap_closure: true
requirements:
  - LINK-01
  - LINK-02
  - LINK-03
  - LINK-04

must_haves:
  truths:
    - "Creating an anchor on a Camera node stores 'NoOp' in the anchor's hidden knob using canSetInput probe, not channel-name heuristic"
    - "Creating an anchor on a Read node stores 'PostageStamp' in the anchor's hidden knob using canSetInput probe"
    - "Creating an anchor on any unknown 2D node stores 'PostageStamp' via canSetInput probe"
    - "Creating an anchor on any unknown 3D/geo node stores 'NoOp' via canSetInput probe"
    - "Creating an anchor on any unknown Deep node stores 'NoOp' via canSetInput probe"
    - "Scratch nodes (NoOp, Scene, DeepMerge) created during probing are deleted immediately"
    - "All existing tests continue to pass — no regressions"
  artifacts:
    - path: "link.py"
      provides: "probe_stream_type_via_can_set_input(), detect_link_class_for_node() updated to use probe"
      exports: ["probe_stream_type_via_can_set_input", "detect_link_class_for_node"]
    - path: "anchor.py"
      provides: "_store_link_class_on_anchor() probes anchor.input(0) when input_node is None"
  key_links:
    - from: "anchor.py _store_link_class_on_anchor()"
      to: "link.py detect_link_class_for_node()"
      via: "calls detect_link_class_for_node(input_node) — input_node may be None"
      pattern: "detect_link_class_for_node\\(input_node\\)"
    - from: "link.py detect_link_class_for_node()"
      to: "probe_stream_type_via_can_set_input()"
      via: "fallback path for unknown node classes"
      pattern: "probe_stream_type_via_can_set_input"
---

<objective>
Replace the channel-name heuristic in detect_link_class_for_node() with a
canSetInput() probe so that anchor creation correctly classifies arbitrary
Nuke node types (2D, 3D/geo, Deep) — not just the 8 types in LINK_CLASSES.

Purpose: The UAT failure showed that camera nodes worked only by coincidence.
Any node class not in LINK_CLASSES and not producing rgba/depth/forward
channels fell through to NoOp accidentally. The canSetInput() API lets Nuke
itself answer the stream-type question definitively.

Output: Updated link.py with probe_stream_type_via_can_set_input() and
updated detect_link_class_for_node(); updated anchor.py with corrected
_store_link_class_on_anchor() that probes via the anchor node's input when
input_node is None.
</objective>

<execution_context>
@/home/latuser/.claude/get-shit-done/workflows/execute-plan.md
@/home/latuser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-copy-paste-semantics/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from current codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From link.py (current implementation to replace):
```python
_2D_CHANNEL_PREFIXES = ('rgba', 'depth', 'forward')

def detect_link_class_for_node(source_node):
    if source_node is None:
        return 'NoOp'
    node_class = source_node.Class()
    if node_class == 'Dot':
        return 'Dot'
    if node_class in LINK_CLASSES:
        return LINK_CLASSES[node_class]
    try:
        channels = source_node.channels()
        if not channels:
            return 'NoOp'
        for channel_name in channels:
            if channel_name.startswith(_2D_CHANNEL_PREFIXES):
                return 'PostageStamp'
        return 'NoOp'
    except Exception:
        return 'NoOp'
```

From anchor.py (current implementation to update):
```python
def _store_link_class_on_anchor(anchor_node, input_node):
    try:
        detected_link_class = detect_link_class_for_node(input_node)
        if ANCHOR_LINK_CLASS_KNOB_NAME in anchor_node.knobs():
            anchor_node[ANCHOR_LINK_CLASS_KNOB_NAME].setValue(detected_link_class)
        else:
            link_class_knob = nuke.String_Knob(ANCHOR_LINK_CLASS_KNOB_NAME)
            link_class_knob.setVisible(False)
            anchor_node.addKnob(link_class_knob)
            anchor_node[ANCHOR_LINK_CLASS_KNOB_NAME].setValue(detected_link_class)
    except Exception:
        pass
```

Nuke API facts:
- nuke.createNode('NoOp')   → creates a 2D-stream scratch node
- nuke.createNode('Scene')  → creates a 3D/geo-stream scratch node
- nuke.createNode('DeepMerge') → creates a Deep-stream scratch node
- anchor_node.canSetInput(input_index, candidate_node) → True if connection
  is valid; False otherwise
- Scratch nodes must be deleted with nuke.delete(node) immediately after
  probing to avoid polluting the script
- LINK_CLASSES fast-path must be preserved as-is (Dot early-return too)
- Probe result mapping: 2D accepted → 'PostageStamp'; 3D or Deep accepted → 'NoOp'; no match → 'NoOp'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add probe_stream_type_via_can_set_input() and replace channel-inspection fallback</name>
  <files>link.py</files>
  <behavior>
    - probe_stream_type_via_can_set_input(anchor_node): creates scratch NoOp, Scene, DeepMerge nodes; calls anchor_node.canSetInput(0, scratch) for each; returns 'PostageStamp' if NoOp accepted, 'NoOp' if Scene or DeepMerge accepted, 'NoOp' if no scratch accepted; deletes all scratch nodes in a finally block regardless of result or exception
    - detect_link_class_for_node(source_node) with None input → 'NoOp' (unchanged)
    - detect_link_class_for_node(Dot node) → 'Dot' (unchanged)
    - detect_link_class_for_node(Read node) → 'PostageStamp' via LINK_CLASSES fast-path (unchanged)
    - detect_link_class_for_node(Camera node) → 'NoOp' via LINK_CLASSES fast-path (unchanged, was already correct by dict)
    - detect_link_class_for_node(unknown 2D node) → 'PostageStamp' via probe (replaces channel-prefix heuristic)
    - detect_link_class_for_node(unknown 3D node) → 'NoOp' via probe (replaces channel-prefix heuristic)
    - Any exception inside probe → 'NoOp' fallback (LINK-04 preserved)
  </behavior>
  <action>
    In link.py:

    1. Remove the _2D_CHANNEL_PREFIXES module-level constant — it is no longer needed.

    2. Add the function probe_stream_type_via_can_set_input(anchor_node) immediately
       before detect_link_class_for_node(). The function must:
       - Create three scratch nodes with nuke.createNode(): NoOp (2D), Scene (3D/geo),
         DeepMerge (Deep). Store all three in local variables immediately so the
         finally block can delete them even if a later creation fails.
       - Wrap creation and probing in try/except/finally. In the finally block,
         call nuke.delete(scratch) for each scratch node that was successfully
         created (guard with `if scratch is not None`).
       - Probe order: NoOp first (2D), then Scene (3D), then DeepMerge (Deep).
       - Call anchor_node.canSetInput(0, scratch_node) for each.
       - Return 'PostageStamp' on first True from NoOp probe.
       - Return 'NoOp' on first True from Scene or DeepMerge probe.
       - Return 'NoOp' if no probe returns True or any exception occurs.
       - The function signature takes anchor_node (not source_node) because
         canSetInput() is called on the anchor — the node whose input type we
         are interrogating.

    3. Replace the channel-inspection block in detect_link_class_for_node().
       The function currently falls through to channel inspection after the
       LINK_CLASSES lookup. Replace that try/except block with a call to
       probe_stream_type_via_can_set_input(source_node). The LINK_CLASSES
       fast-path and Dot early-return at the top stay exactly as-is.

    NOTE: probe_stream_type_via_can_set_input() receives source_node as its
    argument (renamed from anchor_node in the description above to match the
    call site). The parameter name in the implementation should be node_to_probe
    or similar to avoid confusion — the key point is that canSetInput() is
    called on that same node to test whether scratch nodes can connect to it.
    This is the correct API usage: we ask "can this node accept a 2D input?"
  </action>
  <verify>
    <automated>cd /home/latuser/git/nuke_layout_project/paste_hidden && python -m pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    probe_stream_type_via_can_set_input() exists in link.py; detect_link_class_for_node()
    no longer references _2D_CHANNEL_PREFIXES or channels() API; all existing tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update _store_link_class_on_anchor() to probe via anchor.input(0) when input_node is None</name>
  <files>anchor.py</files>
  <behavior>
    - _store_link_class_on_anchor(anchor_node, input_node=None) with a real input_node → passes input_node to detect_link_class_for_node() (existing behavior, unchanged)
    - _store_link_class_on_anchor(anchor_node, input_node=None) with input_node=None and anchor has no input → stores 'NoOp' (same safe fallback as before)
    - _store_link_class_on_anchor(anchor_node, input_node=None) with input_node=None but anchor.input(0) exists → passes anchor.input(0) to detect_link_class_for_node() so the probe runs against the connected node
    - Stored value is correct: 2D input → 'PostageStamp'; 3D/Deep input → 'NoOp'
  </behavior>
  <action>
    In anchor.py, update _store_link_class_on_anchor() to resolve the node
    to detect from before calling detect_link_class_for_node():

    If input_node is None, attempt to get anchor_node.input(0). If that
    returns a node, use it as the node to detect from. If it also returns
    None, pass None to detect_link_class_for_node() (which returns 'NoOp'
    as before — LINK-04 preserved).

    The updated logic before the detect call should be approximately:
      node_to_classify = input_node if input_node is not None else anchor_node.input(0)
      detected_link_class = detect_link_class_for_node(node_to_classify)

    The rest of _store_link_class_on_anchor() (knob creation/update logic)
    stays identical. The outer try/except also stays — any exception silently
    passes so anchor creation is never disrupted.
  </action>
  <verify>
    <automated>cd /home/latuser/git/nuke_layout_project/paste_hidden && python -m pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    _store_link_class_on_anchor() resolves node_to_classify from anchor.input(0)
    when input_node is None; all existing tests pass.
  </done>
</task>

</tasks>

<verification>
After both tasks:
- link.py contains probe_stream_type_via_can_set_input() and no reference to _2D_CHANNEL_PREFIXES
- anchor.py _store_link_class_on_anchor() resolves node_to_classify from anchor.input(0) when input_node is None
- All tests pass: cd /home/latuser/git/nuke_layout_project/paste_hidden && python -m pytest tests/ -q
</verification>

<success_criteria>
- probe_stream_type_via_can_set_input() correctly maps 2D → 'PostageStamp', 3D/Deep → 'NoOp', unknown → 'NoOp'
- detect_link_class_for_node() delegates unknown node classes to the probe instead of channel inspection
- _store_link_class_on_anchor() probes via anchor.input(0) when no explicit input_node is given
- LINK_CLASSES fast-path and Dot early-return are preserved (no regressions)
- No scratch nodes left in script after any probe invocation
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-copy-paste-semantics/01-03-SUMMARY.md`
</output>
